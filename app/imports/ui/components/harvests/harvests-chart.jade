template(name="harvestsChart")
  if activeHarvest
    h3
      = activeHarvest.name
      .pull-right
        = harvestDate
        
    p.fake-notice This is a fake chart, in the future this will show harvest success metrics.
    #chart
      svg
  else
    h3 Please select a data source

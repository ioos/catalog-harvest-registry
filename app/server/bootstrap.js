// Depreciated.  Use /imports/startup to define lazy loaded process.
// This will be kept here for legacy Meteor 1.2 and in the off case when
// it makes sense to use.
Meteor.startup(function () {
});
